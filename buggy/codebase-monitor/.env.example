# Codebase Monitor Configuration

# Server Port
PORT=3000

# Path to watch for changes (defaults to current directory)
WATCH_PATH=.

# Git polling interval in milliseconds (default: 30000)
GIT_POLL_INTERVAL=30000
